server:
  port: 8080

storage:
  type: sqlite
  sqlite:
    path: /app/data/alerts.db

slack:
  enabled: false

pagerduty:
  enabled: false

alerting:
  deduplication_window: 5m
  resend_interval: 30m

logging:
  level: debug
  format: json
